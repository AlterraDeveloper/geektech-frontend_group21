export default class MenuItem {
  constructor(picture, name, price, text, category) {
    this.Picture = picture;
    this.Name = name;
    this.Price = price;
    this.Text = text;
    this.Category = category;
  }
}
